class Solution {
public:
    double distance(vector<int>& x1, vector<int>& x2)
    {
        return  sqrt(pow(x1[0] - x2[0], 2) - pow(x1[1] - x2[1], 2));
    }
    bool validSquare(vector<int>& p1, vector<int>& p2, vector<int>& p3, vector<int>& p4) 
    {
        int length1 = distance(p1, p2);
        int length2=
    }
};